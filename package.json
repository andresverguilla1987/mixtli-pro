{
  "name": "mixtli-pro",
  "version": "1.0.1",
  "description": "Patch de rutas de usuarios (modelo Prisma: Usuario)",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "postinstall": "npx prisma generate",
    "build:deploy": "npm install && npx prisma generate && npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}