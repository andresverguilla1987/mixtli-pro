{
  "name": "mixtli-api",
  "version": "1.0.0",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build:render": "npx prisma generate && npx prisma migrate deploy",
    "db:push": "npx prisma db push",
    "migrate:deploy": "npx prisma migrate deploy",
    "seed": "node prisma/seed.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "morgan": "^1.10.0",
    "dotenv": "^16.4.5",
    "multer": "^1.4.5-lts.1",
    "@aws-sdk/client-s3": "^3.645.0",
    "@aws-sdk/s3-request-presigner": "^3.645.0",
    "bcryptjs": "^2.4.3",
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}