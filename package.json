{
  "name": "mixtli-pro",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "build": "npm ci && npx prisma generate",
    "start": "npx prisma db push && node src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "@prisma/client": "^5.16.1"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}