# Mixtli API (Ready for Render)

## Variables de entorno
- `DATABASE_URL` (PostgreSQL)

## Comandos (local)
```bash
npm install
npx prisma db push
npm run seed
npm start
```

## Render
- **Build Command**: `npm install && npm run build:render`
- **Start Command**: `node server.js`
