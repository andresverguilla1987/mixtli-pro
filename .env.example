# PostgreSQL
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public

# Server
PORT=10000

# CORS - lista separada por comas. Usa * para permitir todos (no recomendado en prod)
CORS_ORIGINS=http://localhost:5173,https://tu-frontend.com
