# === Required for R2/S3 ===
S3_BUCKET=your-bucket-name
AWS_REGION=auto
S3_ENDPOINT=                 # leave empty for AWS S3; for R2 set https://<account>.r2.cloudflarestorage.com
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret

# === CORS ===
# Comma-separated list of allowed origins (no spaces)
ALLOWED_ORIGINS=https://lovely-bienenstitch-6344a1.netlify.app,https://meek-alfajores-1c364d.netlify.app

# === Optional ===
SIGNED_URL_TTL=3600
DEFAULT_PREFIX=public/       # key prefix for new uploads
PORT=10000
